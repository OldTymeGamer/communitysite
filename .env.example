# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-long-random-secret-key-here-make-it-very-long-and-secure

# Database
MONGODB_URI=mongodb://localhost:27017/community
# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/community?retryWrites=true&w=majority

# Discord OAuth & Bot Configuration
DISCORD_CLIENT_ID=your-discord-application-client-id
DISCORD_CLIENT_SECRET=your-discord-application-client-secret
DISCORD_BOT_TOKEN=your-discord-bot-token
DISCORD_GUILD_ID=your-discord-server-guild-id
DISCORD_ADMIN_ROLE_IDS=role_id_1,role_id_2,role_id_3

# Game Server Configuration (RedM/FiveM)
SERVER_API_KEY=your-server-api-key
GAME_SERVER_IP=your-game-server-ip
GAME_SERVER_PORT=your-game-server-port

# Public environment variables (accessible in browser)
NEXT_PUBLIC_GAME_SERVER_IP=your-game-server-ip
NEXT_PUBLIC_GAME_SERVER_PORT=your-game-server-port

# Email Configuration (for verification and password reset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com

# Email Setup Instructions:
# For Gmail:
# 1. Enable 2-factor authentication on your Google account
# 2. Go to Google Account settings > Security > App passwords
# 3. Generate an "App Password" for this application
# 4. Use the 16-character app password as SMTP_PASS (not your regular password)
#
# Alternative SMTP providers:
# - SendGrid: smtp.sendgrid.net (port 587)
# - Mailgun: smtp.mailgun.org (port 587) 
# - Outlook: smtp-mail.outlook.com (port 587)
# - Yahoo: smtp.mail.yahoo.com (port 587)
# - AWS SES: email-smtp.region.amazonaws.com (port 587)

# Discord Setup Instructions:
# 1. Go to https://discord.com/developers/applications
# 2. Create a new application or select existing one
# 3. Get Client ID and Client Secret from OAuth2 section
# 4. Add redirect URI: http://localhost:3000/api/auth/callback/discord
# 5. For bot token: Go to Bot section and create/copy bot token
# 6. For Guild ID: Enable Developer Mode in Discord, right-click your server, Copy ID
# 7. For Role IDs: Right-click roles in server settings, Copy ID (comma-separated for multiple)